{
  "dependencies": {
    "typescript": "^6.0.3"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^30.4.2",
    "ts-jest": "^29.4.11",
    "ts-jest-resolver": "^2.0.1"
  },
  "scripts": {
    "test": "jest",
    "compile": "tsc --project tsconfig.compile.json",
    "clean": "rm -rf dist/*"
  }
}
